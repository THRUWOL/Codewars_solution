/*
[8 kyu] Convert a Number to a String!

We need a function that can transform a number into a string.
*/

using System;

public class Kata
{
  public static string NumberToString(int num) => Convert.ToString(num);
}
